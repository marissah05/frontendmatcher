You are refining the layout and panel resizing behavior of a React app that already uses ResizablePanelGroup.

--------------------------------------------------
GOAL
--------------------------------------------------

Improve the usability and polish of the layout:
1) Make panels resize smoothly and predictably
2) Prevent panels from collapsing too small
3) Improve visual separation between panels
4) Keep everything responsive and clean

--------------------------------------------------
CONSTRAINTS
--------------------------------------------------

- DO NOT change business logic
- DO NOT change backend
- Only modify layout and styling
- Use existing ResizablePanelGroup system
- Keep Tailwind styling consistent

--------------------------------------------------
TASKS
--------------------------------------------------

### 1. IMPROVE PANEL SIZING

Current:
ResizablePanel defaultSize={75} minSize={30}
ResizablePanel defaultSize={25} minSize={15}

Update to:

Left panel (PNMs table):
- defaultSize={70}
- minSize={40}

Right panel (Active Pool):
- defaultSize={30}
- minSize={20}

Reason:
Prevents panels from collapsing too small and breaking UI

--------------------------------------------------

### 2. MAKE RESIZE HANDLE MORE VISIBLE

Update ResizableHandle:

- Increase hit area
- Add subtle visual indicator

Example:

<ResizableHandle
  withHandle
  className="w-2 bg-slate-200 hover:bg-slate-300 transition-colors"
/>

--------------------------------------------------

### 3. PREVENT CONTENT OVERFLOW

Ensure both panels:

- Use flex + overflow-hidden at container level
- Use ScrollArea properly inside panels

Verify:

- No content spills outside
- Tables scroll instead of breaking layout

--------------------------------------------------

### 4. ADD CLEAR SECTION HEADERS

Improve visual hierarchy:

Left panel:
- Add subtle header bar with title (e.g., "PNMs")

Right panel:
- Already has "Active Pool" → make it consistent

Use:

className="px-3 py-2 border-b bg-slate-50 text-xs font-semibold text-slate-600"

--------------------------------------------------

### 5. OPTIONAL: LOCK MIN WIDTH FOR SIDEBAR

Ensure right panel (actives) does not shrink too much:

- Add min width via Tailwind:
  min-w-[220px]

--------------------------------------------------

### 6. ENSURE RESPONSIVENESS

- Panels should not break on smaller screens
- Layout should wrap cleanly if needed
- No horizontal scrolling on full page

--------------------------------------------------

### OUTPUT
--------------------------------------------------

1) Show updated ResizablePanelGroup section
2) Show updated ResizableHandle
3) Brief explanation of improvements
4) STOP and wait for approval